# trained_model

This directory is a standalone Git repository for model artifacts.

Suggested contents:
- model weights
- config files
- tokenizer files if you want to version them with the model

The parent `littleGPT` repository ignores this directory, so model history stays separate from code history.
